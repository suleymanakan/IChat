import React, {useEffect} from 'react';

const CommonWrapper = ({children}) => {
  useEffect(() => {
    //
  }, []);

  return (
    <>
      {children}
    </>
  );
};

export default CommonWrapper;
